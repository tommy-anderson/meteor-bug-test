FROM /Layerfiles/base-app
#meteor is used only for preview links, so no need for it if no PRs
RUN REPEATABLE pkill node; cp -r ./ /root/server/ && tree /root/server/.meteor/local && cd /root/server && nohup meteor npm run start-dev& meteor npm run wait-on-dev
# RUN cd /root && mkdir cpu-profiling && bash ./app/layerfiles/scripts/cpu-profile.bash &true 
RUN true
CHECKPOINT
RUN true
CHECKPOINT disabled
#Show contents after the app has run
RUN tree /root/server/.meteor/local